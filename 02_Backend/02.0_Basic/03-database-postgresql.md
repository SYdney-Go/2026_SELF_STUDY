# 범위 3: PostgreSQL + JPA

> DB 연결 범위에서 채웁니다.

## 미리볼 구조

```text
EventController → EventService → EventRepository → PostgreSQL
```

## 핵심 질문 (나중에 답할 것)

- Entity와 DB 테이블은 어떻게 연결되나?
- JPA Repository에서 `findById`는 누가 만들어 주나?
- `application.properties`에 DB URL은 어떻게 적나?
